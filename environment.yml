#
# Conda environment definition with dependencies
#
name: plinder
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  - python=3.10.*
  - reduce
  - openstructure
  - mmseqs2
  - foldseek
  - plip=2.3.0
  - pip:
    - keyrings.google-artifactregistry-auth==1.1.2
